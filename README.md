This is the submission repository of the Xeno "Take-Home: Comm-Log Send Reconciliation" Assignment.
The files contained in the repository and their explanation:
full_session.txt contains the whole sqlite3 shell scripts along with their corresponding outputs.

outputs.txt contains all the outputs returned at each query until the final output.

queries.sql contains every query that led to the final output.

reconciliation_bridge.xlxs contains the table with Step | Description | Result | Reason ; fields that describe each step from first to final, providing their clarifications.

Tool used: sqlite3

Database used: comm_log.db

Tables: communication_log campaign
